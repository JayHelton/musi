/**
 * Fretboard Interval Map — browser entry point.
 * Route id remains `intervalorbit` for bookmark / storage compatibility.
 * Optional hash alias: #intervalmap
 */

export { initIntervalMap as initIntervalOrbit, stopIntervalMap as stopIntervalOrbit } from './interval-map/ui.js';
